#include <string>
#include <vector>

using namespace std;

int solution(int N, int number) {
    int answer = 0;
    vector<vector<int>> v;
    vector<int> zero;
    zero.push_back(0);
    v.push_back(zero);
    for(int i=1;i<8;i++){
        vector<int> tmp;
        
    }
    return answer;
}
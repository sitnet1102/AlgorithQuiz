#include <string>
#include <vector>

using namespace std;

vector<int> num;
vector<bool> isused;
vector<string> v;
int n;

void func(int k){

    if(k == n){

    }

    for(int i=0;i<n)
}

string solution(vector<int> numbers) {
    string answer = "";
    
    return answer;
}
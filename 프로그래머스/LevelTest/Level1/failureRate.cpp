#include <string>
#include <vector>
#include <algorithm>

using namespace std;

vector<int> solution(int N, vector<int> stages) {
    vector<int> answer;
    vector<int> v;
    sort(stages.begin(), stages.end());
    for(int i=1;i<=N;i++){
        int m;
        for(int j=0;j<stages.size();j++){
            m = j+1;
            if()
        }
    }
    return answer;
}